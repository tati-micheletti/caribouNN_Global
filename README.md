After knitting and rendering `caribouNN_Global.Rmd`, overwrite this file with a symlink to `caribouNN_Global.md`.

E.g., on Linux/macOS from terminal:

```bash
cd C:/Users/Tati/GitHub/caribouNN_Global
rm README.md && ln -s caribouNN_Global.md README.md
```
